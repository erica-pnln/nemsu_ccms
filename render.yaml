services:
  - type: web
    name: nemsu-ccms
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: nemsu_ccms_db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
